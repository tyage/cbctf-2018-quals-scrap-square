doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    title
  body
    header
      nav
        if session.user
          a(href='/') Top
          a(href='/new') New Scrap
          a(href='/logout') Logout
        else
          a(href='/login') Login
          a(href='/register') Register

    if errors
      div.errors
        each error in errors
          p!= error

    div
      block content

    block scripts
